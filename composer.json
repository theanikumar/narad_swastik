{
    "name": "narad/swastik-api",
    "description": "NARAD-SWASTIK Mining ERP REST API",
    "type": "project",
    "require": {
        "php": ">=8.1",
        "firebase/php-jwt": "^6.10",
        "vlucas/phpdotenv": "^5.6"
    },
    "autoload": {
        "psr-4": {
            "App\\": "app/"
        }
    },
    "config": {
        "policy": {
            "advisories": {
                "block": false
            }
        }
    },
    "scripts": {
        "migrate": "php database/migrate.php"
    }
}
